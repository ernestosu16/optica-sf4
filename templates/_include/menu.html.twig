<nav class="col-md-2 d-none d-md-block bg-light sidebar">
    <div class="sidebar-sticky">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a id="home" class="nav-link" href="{{ path('home') }}"><span><i class="fas fa-home"></i> Inicio</span></a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a id="home" class="nav-link" href="{{ path('sonata_admin_dashboard') }}"><span><i
                                    class="fas fa-cog"></i> Administración</span></a>
                </li>
            {% endif %}
            {% if app.user %}
                <li class="nav-item">
                    <a id="login" class="nav-link" href="{{ path('sonata_user_admin_security_logout') }}"><span><i class="fas fa-lock"></i> Salir</span></a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a id="login" class="nav-link" href="{{ path('sonata_user_admin_security_login') }}"><span><i class="fas fa-lock-open"></i> Acceso</span></a>
                </li>
            {% endif %}
        </ul>
        <hr>
    </div>
</nav>